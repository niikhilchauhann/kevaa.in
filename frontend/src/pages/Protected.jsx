export default function Protected() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>Welcome to the protected page!</h1>
      <p>Only accessible after login (via "Discover our collection")</p>
    </div>
  );
}
